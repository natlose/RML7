<UserControl x:Class="AlkalmazasKeret.TortenetValto_N"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AlkalmazasKeret"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--
        Ne példányosíts itt Tortenetek_NM nézetmodellt!
        Azt már létrehozta a Tortenetek_N, a FoAblak_N köti majd hozzá a DataContext-et!
    -->
    <ListBox ItemsSource="{Binding Tortenetek}" SelectedValuePath="Azonosito" SelectedValue="{Binding Aktiv, Mode=TwoWay}">
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel MaxWidth="{Binding ActualWidth, Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBox}}}" />
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Border BorderThickness="1" CornerRadius="2" BorderBrush="Black">
                    <TextBlock Text="{Binding Azonosito, StringFormat=HH:mm:ss}"/>
                </Border>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
